// DEPRECATED: `tailwind.config.js` is now the single source of truth for colors.
// Keep this file as a placeholder to avoid accidental imports. Do not use.

const theme: Record<string, unknown> = {};

export default theme;
